# Megachat
## Message API (Django REST Framework)

این پروژه یک API ساده با **Django REST Framework** است که امکان **ایجاد پیام** و **دریافت لیست پیام‌ها** را فراهم می‌کند.  
همچنین یک **ربات تلگرام Echo Bot** نیز دارد که هر پیامی دریافت کند، همان پیام را برمی‌گرداند.

---

## ✅ Features

### 🔹 Django REST API
- ساخت پیام جدید (**POST**)
- دریافت لیست پیام‌ها (**GET**)
- ذخیره پیام‌ها در دیتابیس (**SQLite** یا **PostgreSQL**)
- مدل پیام شامل:
  - متن پیام
  - تاریخ ایجاد
  - وضعیت (مثل خوانده شده / فعال)

### 🔹 Telegram Echo Bot
- دریافت پیام از کاربر
- ارسال همان پیام به کاربر (Echo)

---

## ✅ Tech Stack
- Python
- Django
- Django REST Framework
- SQLite (پیش‌فرض)
- telegram_bot (برای ربات تلگرام)

---

## ✅ Installation

### 1) کلون پروژه
```bash
git clone <YOUR_REPO_URL>
cd <PROJECT_FOLDER>
